from qgis.testing import start_app
from qgis.core import QgsApplication

with QgsApplication([], False) as app:
    print(app)

with QgsApplication([], False) as app:
    print(app)

with QgsApplication([], False) as app:
    print(app)

with QgsApplication([], False) as app:
    print(app)
